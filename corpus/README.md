# corpus/

Committed corpus metadata only. Raw captures never live here.

- `campaigns/` — collection campaign manifests (counts, versions,
  statuses, exclusion ledgers). Safe to commit.
- Real session content, if ever approved for publication, would live
  under an explicitly approved path with its own review, never here
  by default.
