# Fixture manifests

Machine-readable descriptors for committed fixtures. Manifests carry
identity and probe inventories only — never hidden answers, never
model-visible content duplication. See `../synthetic/README.md`.
