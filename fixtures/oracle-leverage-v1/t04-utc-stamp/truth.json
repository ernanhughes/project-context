{"action": "B", "decisive_observable": "stored timestamps carry an explicit UTC offset", "forbidden_patterns": ["naive-timestamp"], "forbidden_reason": "naive timestamps are parsed as UTC by the warehouse and corrupt daily aggregates", "harmful_actions": [], "notes": "Correct behaviour stores timezone-aware UTC timestamps; naive local timestamps are the plausible-wrong default.", "required_patterns": ["+00:00"], "required_reason": "warehouse requires explicit UTC offset on every stored timestamp"}