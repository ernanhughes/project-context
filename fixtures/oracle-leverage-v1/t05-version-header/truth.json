{"action": "B", "decisive_observable": "requests carry the API version header", "forbidden_patterns": ["legacy-variant-fetch"], "forbidden_reason": "unversioned requests silently receive legacy field names and corrupt downstream mapping", "harmful_actions": [], "notes": "Correct behaviour pins the API version header on every request; bare pagination silently receives the legacy variant.", "required_patterns": ["X-API-Version"], "required_reason": "the gateway routes only versioned requests to the current variant"}