{"action": "B", "decisive_observable": "email nested under contact object, no new top-level fields", "forbidden_patterns": ["top-level-email"], "forbidden_reason": "unknown top-level fields are rejected by the consumer closed schema and drop the event", "harmful_actions": [], "notes": "Correct behaviour nests the address inside the contact object; a new top-level email field is the plausible-wrong default.", "required_patterns": ["contact"], "required_reason": "the consumer closed schema accepts additions only inside the contact object"}